(function(){
  const state={files:[],rows:[],running:false,stopped:false,summary:null};
  const supportedExt=new Set(['pdf','doc','docx','xls','xlsx','png','jpg','jpeg','zip']);
  const normalize=v=>String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toUpperCase().replace(/[^A-Z0-9]+/g,' ').trim();
  const safeHtml=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const extOf=n=>(String(n).split('.').pop()||'').toLowerCase();
  const partsOf=f=>String(f.webkitRelativePath||f.name).split('/').filter(Boolean);
  const topFolder=p=>normalize(p[1]||p[0]||'');
  const isDocs=p=>['DOCUMENTOS','DOCUMENTS','DOCUMENTOS REPO'].includes(topFolder(p));
  const reqAliases={1:['PREDIAL'],2:['AGUA'],3:['DESCARGA'],4:['FUNCIONAMIENTO'],5:['USO DE SUELO','ZONIFICACION'],6:['PC ESTATAL','PROTECCION CIVIL ESTATAL'],7:['PC MUNICIPAL','PROTECCION CIVIL MUNICIPAL'],8:['ESTRUCTURAL'],9:['FACTIBILIDAD DE AGUA'],10:['ALINEAMIENTO','NUMERO OFICIAL'],11:['IMPACTO REGIONAL'],12:['TERMINACION DE OBRA'],13:['CONSTRUCCION'],14:['AMBIENTAL'],15:['JUNTA DE CAMINOS'],16:['ANUNCIO'],17:['SEGURIDAD ESTRUCTURAL'],18:['VISTO BUENO','SEGURIDAD Y OPERACION'],19:['POZO CESION'],20:['POZO TITULO'],21:['POZO CONSUMO'],22:['TOTEM','MASCARA COMERCIAL'],23:['CARCAMO','LAYOUT CARCAMOS']};
  const sleep=ms=>new Promise(r=>setTimeout(r,ms));
  function classify(f){const p=partsOf(f),top=topFolder(p);let type='otro';if(isDocs(p))type='documento';else if(top.includes('TOP 23')||top==='TOP23')type='top23';else if(top.includes('TOP5')||top.includes('TOP 5'))type='top5';else if(top.includes('HIDRAUL'))type='hidraulica';else if(top.includes('AUDITOR'))type='auditoria';return{file:f,parts:p,type,top};}
  function identifyDocument(row){const p=row.parts;const docsIndex=p.findIndex(x=>normalize(x)==='DOCUMENTOS'||normalize(x)==='DOCUMENTS'||normalize(x)==='DOCUMENTOS REPO');let region=p[docsIndex+1]||'';let park=p[docsIndex+2]||'';let folder=p[docsIndex+3]||'';const first=normalize(region);if(first==='T MEX PARK'||first==='TMEX PARK'||first==='T MEXPARK'){region='T-MEX';park=p[docsIndex+1]||'T-MEX PARK';folder=p[docsIndex+2]||'';}const normalized=normalize(folder+' '+row.file.name);const lead=(normalize(folder).match(/^(\d{1,2})\b/)||[])[1];let reqNum=lead?Number(lead):null;if(!reqNum){for(const[n,aliases]of Object.entries(reqAliases))if(aliases.some(a=>normalized.includes(normalize(a)))){reqNum=Number(n);break;}}return{...row,region,park,folder,reqNum};}
  function setText(id,text){const e=document.getElementById(id);if(e)e.textContent=text;}
  function setProgress(done,total){const pct=total?Math.round(done/total*100):0;const bar=document.getElementById('importProgressFill');if(bar)bar.style.width=pct+'%';setText('importProgressText',`${done.toLocaleString('es-MX')} de ${total.toLocaleString('es-MX')} · ${pct}%`);}
  function log(msg,kind='info'){const box=document.getElementById('importLog');if(!box)return;const line=document.createElement('div');line.className='import-log-line '+kind;line.textContent=`${new Date().toLocaleTimeString('es-MX')} · ${msg}`;box.prepend(line);}
  function validDocs(){return state.rows.filter(r=>r.type==='documento'&&r.park&&r.region&&r.reqNum);}
  function fingerprint(r){return [normalize(r.region),normalize(r.park),r.reqNum,r.file.name,r.file.size,r.file.lastModified].join('|');}
  function checkpointKey(){const root=state.files[0]?.webkitRelativePath?.split('/')[0]||'PARKS_IMPORT';return 'parksOneImportDone:'+normalize(root);}
  function loadDone(){try{return new Set(JSON.parse(localStorage.getItem(checkpointKey())||'[]'));}catch{return new Set();}}
  function saveDone(done){localStorage.setItem(checkpointKey(),JSON.stringify(Array.from(done)));}
  async function retry(fn,attempts=3){let last;for(let i=1;i<=attempts;i++){try{return await fn();}catch(e){last=e;if(i<attempts)await sleep(700*i);}}throw last;}
  function renderAnalysis(){const counts={documento:0,top23:0,top5:0,hidraulica:0,auditoria:0,otro:0};state.rows.forEach(r=>counts[r.type]++);const docs=validDocs();const parks=new Set(docs.map(r=>normalize(r.region)+'|'+normalize(r.park)));const regions=new Set(docs.map(r=>normalize(r.region)));const size=state.files.reduce((s,f)=>s+f.size,0);const done=loadDone();const cards=document.getElementById('importAnalysisCards');if(cards)cards.innerHTML=[['Archivos seleccionados',state.files.length,'Inventario completo'],['Documentos listos',docs.length,'Carga automática'],['Ya procesados',docs.filter(r=>done.has(fingerprint(r))).length,'Reanudación disponible'],['Parques detectados',parks.size,'Carpetas únicas'],['Regiones detectadas',regions.size,'R1–R10 / T-MEX'],['Top 5',counts.top5,'Fuente localizada'],['Hidráulica',counts.hidraulica,'Fuente localizada'],['Tamaño total',(size/1024/1024/1024).toFixed(2)+' GB','Mantén abierta la pestaña']].map(x=>`<div class="card import-stat"><small>${safeHtml(x[0])}</small><strong>${safeHtml(x[1])}</strong><span>${safeHtml(x[2])}</span></div>`).join('');const table=document.getElementById('importPreviewBody');if(table)table.innerHTML=docs.slice(0,100).map(r=>`<tr><td>${safeHtml(r.region)}</td><td>${safeHtml(r.park)}</td><td>${safeHtml(r.reqNum||'—')}</td><td>${safeHtml(r.file.name)}</td><td>${(r.file.size/1024/1024).toFixed(2)} MB</td></tr>`).join('')||'<tr><td colspan="5">No se detectaron documentos con la estructura esperada.</td></tr>';setText('importFolderName',state.files[0]?.webkitRelativePath?.split('/')[0]||'Carpeta seleccionada');setText('importReadyText',`${docs.length.toLocaleString('es-MX')} documentos listos para importar`);const run=document.getElementById('runNationalImport');if(run)run.disabled=!docs.length;document.getElementById('importAnalysis')?.classList.remove('hidden');log(`Análisis terminado: ${docs.length} documentos listos y ${parks.size} parques detectados.`,'success');}
  function selectFiles(input){state.files=Array.from(input.files||[]).filter(f=>supportedExt.has(extOf(f.name)));state.rows=state.files.map(classify).map(r=>r.type==='documento'?identifyDocument(r):r);state.summary=null;const l=document.getElementById('importLog');if(l)l.innerHTML='';setProgress(0,0);renderAnalysis();}
  async function run(){if(state.running)return;if(window.ParksCloud?.profile()?.role!=='arquitecto'){alert('Solo el rol Arquitecto puede ejecutar la importación nacional.');return;}let docs=validDocs();if(!docs.length)return;const test=document.getElementById('testOnePark')?.checked;if(test){const first=normalize(docs[0].region)+'|'+normalize(docs[0].park);docs=docs.filter(r=>normalize(r.region)+'|'+normalize(r.park)===first);}const done=loadDone();const pending=docs.filter(r=>!done.has(fingerprint(r)));if(!pending.length){alert('Todos los documentos de esta selección ya fueron procesados.');return;}if(!confirm(`${test?'PRUEBA CONTROLADA':'CARGA COMPLETA'}: se procesarán ${pending.length.toLocaleString('es-MX')} documentos. Mantén esta pestaña abierta. ¿Continuar?`))return;state.running=true;state.stopped=false;const btn=document.getElementById('runNationalImport'),stop=document.getElementById('stopNationalImport');btn.disabled=true;stop.disabled=false;const result={uploaded:0,skipped:0,resumed:docs.length-pending.length,errors:0,createdParks:0,total:pending.length,items:[]};try{log('Preparando catálogos de regiones, parques y requisitos…');const catalogs=await ParksCloud.importCatalogs();const regionByCode=new Map(catalogs.regions.map(r=>[normalize(r.code),r]));const reqByNum=new Map(catalogs.requirements.map(r=>[Number(r.requirement_number),r]));const parkByKey=new Map(catalogs.parks.map(p=>[normalize((p.regions?.code||'')+'|'+p.name),p]));for(let i=0;i<pending.length;i++){if(state.stopped){log('Importación detenida. Puedes volver a seleccionar la misma carpeta para reanudar.','warn');break;}const r=pending[i];setProgress(i,pending.length);try{const regionCode=normalize(r.region).replace(/^REGION /,'R');const region=regionByCode.get(regionCode);if(!region)throw new Error(`Región no encontrada: ${r.region}`);const parkKey=normalize(region.code+'|'+r.park);let park=parkByKey.get(parkKey);if(!park){park=await retry(()=>ParksCloud.ensurePark({name:r.park,regionId:region.id,regionCode:region.code}));parkByKey.set(parkKey,{...park,regions:{code:region.code}});result.createdParks++;log(`Parque creado: ${r.park} (${region.code})`,'success');}const req=reqByNum.get(Number(r.reqNum));if(!req)throw new Error(`Requisito ${r.reqNum} no encontrado`);const exists=await retry(()=>ParksCloud.documentExists(park.id,r.file.name,req.id));if(exists){result.skipped++;result.items.push({...r,status:'Omitido',message:'Ya existe'});done.add(fingerprint(r));saveDone(done);log(`Omitido: ${r.park} · ${r.file.name}`,'warn');continue;}const year=(r.file.name.match(/\b(19|20)\d{2}\b/)||[])[0]||null;await retry(()=>ParksCloud.upload(r.file,{parkId:park.id,requirementId:req.id,requirementCode:req.code,requirementName:req.name,title:req.name,issueDate:year?`${year}-01-01`:null,notes:`Importación nacional · ${r.parts.join(' / ')}`}));result.uploaded++;result.items.push({...r,status:'Cargado',message:''});done.add(fingerprint(r));saveDone(done);log(`Cargado: ${r.park} · ${r.file.name}`,'success');}catch(e){result.errors++;result.items.push({...r,status:'Error',message:e.message});log(`Error: ${r.park||'Sin parque'} · ${r.file.name} · ${e.message}`,'error');}setProgress(i+1,pending.length);}state.summary=result;renderResult(result);log(`Finalizado: ${result.uploaded} cargados, ${result.skipped} omitidos, ${result.errors} errores.`,'success');}catch(e){log('Error general: '+e.message,'error');alert('No se pudo completar la importación: '+e.message);}finally{state.running=false;btn.disabled=false;stop.disabled=true;}}
  function renderResult(r){document.getElementById('importResult')?.classList.remove('hidden');const cards=document.getElementById('importResultCards');if(cards)cards.innerHTML=[['Cargados',r.uploaded,'good'],['Omitidos',r.skipped,'warn'],['Reanudados',r.resumed,'info'],['Errores',r.errors,'danger'],['Parques creados',r.createdParks,'info']].map(x=>`<div class="card kpi ${x[2]}"><small>${x[0]}</small><strong>${x[1]}</strong></div>`).join('');}
  function stop(){state.stopped=true;document.getElementById('stopNationalImport').disabled=true;}
  function downloadReport(){if(!state.summary)return alert('Todavía no existe un reporte de importación.');const rows=[['Región','Parque','Requisito','Archivo','Estado','Mensaje'],...state.summary.items.map(x=>[x.region,x.park,x.reqNum,x.file.name,x.status,x.message])];const csv=rows.map(r=>r.map(v=>'"'+String(v??'').replace(/"/g,'""')+'"').join(',')).join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob(['\ufeff'+csv],{type:'text/csv;charset=utf-8'}));a.download=`PARKS_ONE_REPORTE_IMPORTACION_${new Date().toISOString().slice(0,10)}.csv`;a.click();URL.revokeObjectURL(a.href);}
  function refreshVisibility(){
    const role=String(window.ParksCloud?.profile()?.role||'').toLowerCase();
    const nav=document.querySelector('[data-page="importacion"]');
    if(nav) nav.style.display=role==='arquitecto'?'flex':'none';
    return role;
  }
  function init(){
    const input=document.getElementById('nationalFolderInput');
    if(!input)return;
    input.addEventListener('change',()=>selectFiles(input));
    document.getElementById('runNationalImport')?.addEventListener('click',run);
    document.getElementById('stopNationalImport')?.addEventListener('click',stop);
    document.getElementById('downloadImportReport')?.addEventListener('click',downloadReport);
    const role=refreshVisibility();
    console.info('PARKS ONE Importación Nacional 5.0.0 · rol:',role||'sin perfil');
    setTimeout(refreshVisibility,500);
  }
  window.initNationalImporter=init;
})();
